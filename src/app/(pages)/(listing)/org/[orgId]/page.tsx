import React from "react";


export default function OrgListing() {
    return (
        <div>
            {/* {orgs.map(org => (
                            <div key={org.id} className="job-card">
                                <h2>{org.name}</h2>
                                <p>{org.description}</p>
                            </div>
            ))} */}
        </div>
    );
}